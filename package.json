{
  "name": "draftwise-fantasy-draft-tool",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "data:download": "node scripts/download_player_data.mjs",
    "backtest": "vitest run src/lib/backtest.test.ts --reporter=verbose",
    "backtest:historical": "vitest run --config vitest.historical.config.ts --reporter=verbose"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.6",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "jsdom": "^30.0.1",
    "vitest": "latest"
  }
}
